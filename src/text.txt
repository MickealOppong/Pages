@import "./variables";

/* ==========================================================================
   PAGE CONTAINER SYSTEM
   ========================================================================== */
.discover-page {
  width: 100%;
  min-height: 100vh;
  background: #f6f7fb;
  display: flex;
  flex-direction: column;
}

.discover-center {
  max-width: 1700px;
  margin: 0 auto;
  padding: 2rem;
  width: 100%;
  flex: 1;
}

.discover {
  width: 100%;
}

.discover-header {
  position: sticky;
  top: 0;
  z-index: 22;
  margin-bottom: 2rem;
}

/* ==========================================================================
   THE ALTERNATING MAGAZINE ENGINE
   ========================================================================== */
.discover-feed {
  display: grid;
  // Crucial: 3 equal column tracks enable perfect editorial asymmetry
  grid-template-columns: repeat(3, minmax(330px, 1fr));
  gap: 28px;
  align-items: start;

  /* 
     ----------------------------------------------------------------------
     ROW 1 DESIGN PATTERN: [ Portrait ] [ Landscape ] [ Landscape ]
     ---------------------------------------------------------------------- 
  */
  
  // Element 1 (Landscape from data array): Shuffled right into column 2 
  & > :nth-child(6n + 1) {
    grid-column: 1;
  }

  // Element 2 (Portrait from data array): Shuffled left into column 1
  & > :nth-child(6n + 2) {
    grid-column: 2;
    grid-row: 2; // Snaps it upward to start the row
  }

  // Element 3 (Portrait from data array): Cleanly occupies column 3 naturally
  & > :nth-child(6n + 3) {
    grid-column: 2;
  }

  /* 
     ----------------------------------------------------------------------
     ROW 2 DESIGN PATTERN: [ Portrait ] [ Portrait ] [ Landscape ]
     ---------------------------------------------------------------------- 
     Elements 4, 5, and 6 sit perfectly on the second row. Each naturally 
     takes up a single column track width, lining up beautifully.
  */
  & > :nth-child(6n + 4),
  & > :nth-child(6n + 5),
  & > :nth-child(6n + 6) {
    grid-column: span 1;
  }
}

/* ==========================================================================
   EMPTY STATE & FOOTER PAGINATION
   ========================================================================== */
.empty-state {
  display: flex;
  justify-content: center;
  align-items: center;
  min-height: 500px;
  width: 100%;

  .empty-card {
    text-align: center;
    max-width: 450px;
    padding: 3rem 2rem;
    background: #ffffff;
    border-radius: 24px;
    box-shadow: 0 12px 35px rgba(0, 0, 0, 0.04);

    .empty-icon {
      font-size: 3.5rem;
      display: block;
      margin-bottom: 1.25rem;
    }

    h2 {
      font-size: 1.5rem;
      font-weight: 700;
      color: #1a1a1a;
      margin: 0 0 0.5rem 0;
    }

    p {
      font-size: 0.95rem;
      color: #666666;
      line-height: 1.5;
      margin: 0;
    }
  }
}

.discover-pagination {
  margin: 3rem 0;
  display: flex;
  justify-content: center;
  width: 100%;
}

/* ==========================================================================
   RESPONSIVE BREAKPOINTS (Mobile & Tablet Reflow)
   ========================================================================== */
@media (max-width: 1200px) {
  .discover-feed {
    // Falls back seamlessly to 2-columns on smaller monitors/tablets
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  // Clear specific 3-column placement logic for mid-screen layouts
  .discover-feed > :nth-child(6n + 1),
  .discover-feed > :nth-child(6n + 2),
  .discover-feed > :nth-child(6n + 3) {
    grid-column: auto;
    grid-row: auto;
  }
}

@media (max-width: 768px) {
  .discover-center {
    padding: 1rem;
  }

  .discover-feed {
    // Absolute 1-column layout for standard mobile viewports
    grid-template-columns: 1fr;
    gap: 20px;
  }

  .discover-feed > * {
    grid-column: auto !important;
    grid-row: auto !important;
  }
}
